extends layout

block content
  .inner
    h1= title
    table.table
      thead
        td photo
        td ranking
        td name
        td reviews
        td Average rating
      each store, i in stores
        tr
          td
            a(href=`/stores/${store.slug}`)
              img(src=`/uploads/${store.photo || 'store.png'}` width=200 alt=store.name)
          td #{i + 1}
          td: a(href=`/stores/${store.slug}`)= store.name
          td= store.reviews.length
          td
            .review__stars(title=`Rated ${store.averageRating} out of 5 stars`)
              span #{Math.round(store.averageRating * 10) / 10} ★
